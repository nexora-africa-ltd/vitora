"""
Serializer contract tests for imaging_standalone app.

Auto-generated by scripts/generate_contracts.py — DO NOT edit field sets manually.
Re-run the script to update after serializer changes:
    poetry run python scripts/generate_contracts.py
"""

import pytest  # type: ignore

from hmis.apps.imaging.standalone.serializers import (
    ExternalImagingOrderAcceptSerializer,
    ExternalImagingOrderRejectSerializer,
    ExternalImagingOrderRequestSerializer,
    StandaloneImagingOrderCreateSerializer,
    StandaloneImagingOrderItemSerializer,
    WalkInImagingPatientCreateSerializer,
    WalkInImagingPatientSerializer,
)

CONTRACTS: list[tuple[type, frozenset[str]]] = [
    (
        ExternalImagingOrderAcceptSerializer,
        frozenset(
            {
                "auto_create_walkin",
            }
        ),
    ),
    (
        ExternalImagingOrderRejectSerializer,
        frozenset(
            {
                "reason",
            }
        ),
    ),
    (
        ExternalImagingOrderRequestSerializer,
        frozenset(
            {
                "clinical_indication",
                "created_at",
                "external_patient_id",
                "id",
                "imaging_order",
                "message_control_id",
                "patient_dob",
                "patient_gender",
                "patient_id_number",
                "patient_name",
                "patient_phone",
                "placer_order_number",
                "priority",
                "processed_at",
                "processed_by",
                "referring_clinician",
                "referring_clinician_license",
                "rejection_reason",
                "relevant_clinical_history",
                "requested_procedures",
                "sending_application",
                "sending_facility",
                "status",
                "updated_at",
                "walkin_patient",
            }
        ),
    ),
    (
        StandaloneImagingOrderCreateSerializer,
        frozenset(
            {
                "clinical_indication",
                "external_referring_clinician",
                "external_referring_facility",
                "items",
                "patient_id",
                "priority",
                "relevant_clinical_history",
                "walkin_dob",
                "walkin_gender",
                "walkin_name",
                "walkin_national_id",
                "walkin_patient_id",
                "walkin_phone",
            }
        ),
    ),
    (
        StandaloneImagingOrderItemSerializer,
        frozenset(
            {
                "laterality",
                "procedure_code",
                "specific_instructions",
            }
        ),
    ),
    (
        WalkInImagingPatientCreateSerializer,
        frozenset(
            {
                "date_of_birth",
                "email",
                "first_name",
                "gender",
                "id_type",
                "last_name",
                "national_id",
                "phone_number",
                "referring_clinician",
                "referring_facility",
            }
        ),
    ),
    (
        WalkInImagingPatientSerializer,
        frozenset(
            {
                "created_at",
                "date_of_birth",
                "email",
                "first_name",
                "full_name",
                "gender",
                "id",
                "id_type",
                "last_name",
                "linked_patient",
                "national_id",
                "phone_number",
                "referring_clinician",
                "referring_facility",
                "registration_number",
                "updated_at",
            }
        ),
    ),
]


def _serializer_id(val):
    """Return readable test ID for parametrize."""
    return val.__name__ if isinstance(val, type) else None


@pytest.mark.django_db
class TestContractsImagingStandalone:
    """Contract tests for imaging_standalone serializers."""

    @pytest.mark.parametrize(
        "serializer_class, expected_fields",
        CONTRACTS,
        ids=lambda val: _serializer_id(val),
    )
    def test_serializer_fields_match_contract(self, serializer_class, expected_fields):
        """Serializer field set must match the contract exactly."""
        actual_fields = frozenset(serializer_class().fields.keys())

        added = actual_fields - expected_fields
        removed = expected_fields - actual_fields

        assert not added, (
            f"\n{serializer_class.__name__}: new fields added to serializer.\n"
            f"  Added: {sorted(added)}\n"
            f"  → Re-run: poetry run python scripts/generate_contracts.py\n"
            f"  → Update the frontend Zod schema to include these fields"
        )
        assert not removed, (
            f"\n{serializer_class.__name__}: fields removed from serializer.\n"
            f"  Removed: {sorted(removed)}\n"
            f"  → Re-run: poetry run python scripts/generate_contracts.py\n"
            f"  → Update the frontend Zod schema to remove these fields"
        )
