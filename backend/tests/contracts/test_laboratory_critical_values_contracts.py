"""
Serializer contract tests for laboratory_critical_values app.

Auto-generated by scripts/generate_contracts.py — DO NOT edit field sets manually.
Re-run the script to update after serializer changes:
    poetry run python scripts/generate_contracts.py
"""

import pytest  # type: ignore

from hmis.apps.laboratory.critical_values.serializers import (
    CriticalValueComplianceSerializer,
    CriticalValueNotificationDetailSerializer,
    CriticalValueNotificationListSerializer,
    CriticalValueRangeCreateSerializer,
    CriticalValueRangeListSerializer,
    EscalateSerializer,
    NotifySerializer,
    ReadBackSerializer,
)

CONTRACTS: list[tuple[type, frozenset[str]]] = [
    (
        CriticalValueComplianceSerializer,
        frozenset(
            {
                "acknowledged_count",
                "average_notification_minutes",
                "compliance_rate",
                "notified_within_deadline",
                "overdue_count",
                "pending_count",
                "read_back_rate",
                "read_back_verified_count",
                "total_notifications",
            }
        ),
    ),
    (
        CriticalValueNotificationDetailSerializer,
        frozenset(
            {
                "acknowledged_at",
                "created_at",
                "critical_range",
                "critical_value",
                "detected_at",
                "escalated_to",
                "escalation_notes",
                "id",
                "is_overdue",
                "minutes_elapsed",
                "notes",
                "notification_method",
                "notification_time_minutes",
                "notified_at",
                "notified_by",
                "notified_to",
                "notified_to_name",
                "patient_name",
                "read_back_at",
                "read_back_value",
                "read_back_verified",
                "result",
                "severity",
                "status",
                "test_name",
                "updated_at",
            }
        ),
    ),
    (
        CriticalValueNotificationListSerializer,
        frozenset(
            {
                "acknowledged_at",
                "created_at",
                "critical_value",
                "detected_at",
                "id",
                "is_overdue",
                "minutes_elapsed",
                "notification_method",
                "notification_time_minutes",
                "notified_at",
                "notified_by",
                "notified_to_name",
                "patient_name",
                "read_back_verified",
                "result",
                "severity",
                "status",
                "test_name",
            }
        ),
    ),
    (
        CriticalValueRangeCreateSerializer,
        frozenset(
            {
                "critical_high",
                "critical_low",
                "notification_deadline_minutes",
                "panic_high",
                "panic_low",
                "test",
            }
        ),
    ),
    (
        CriticalValueRangeListSerializer,
        frozenset(
            {
                "created_at",
                "critical_high",
                "critical_low",
                "id",
                "is_active",
                "notification_deadline_minutes",
                "panic_high",
                "panic_low",
                "test",
                "test_code",
                "test_name",
                "updated_at",
            }
        ),
    ),
    (
        EscalateSerializer,
        frozenset(
            {
                "escalated_to",
                "notes",
            }
        ),
    ),
    (
        NotifySerializer,
        frozenset(
            {
                "method",
                "notified_to",
                "notified_to_name",
            }
        ),
    ),
    (
        ReadBackSerializer,
        frozenset(
            {
                "read_back_value",
            }
        ),
    ),
]


def _serializer_id(val):
    """Return readable test ID for parametrize."""
    return val.__name__ if isinstance(val, type) else None


@pytest.mark.django_db
class TestContractsLaboratoryCriticalValues:
    """Contract tests for laboratory_critical_values serializers."""

    @pytest.mark.parametrize(
        "serializer_class, expected_fields",
        CONTRACTS,
        ids=lambda val: _serializer_id(val),
    )
    def test_serializer_fields_match_contract(self, serializer_class, expected_fields):
        """Serializer field set must match the contract exactly."""
        actual_fields = frozenset(serializer_class().fields.keys())

        added = actual_fields - expected_fields
        removed = expected_fields - actual_fields

        assert not added, (
            f"\n{serializer_class.__name__}: new fields added to serializer.\n"
            f"  Added: {sorted(added)}\n"
            f"  → Re-run: poetry run python scripts/generate_contracts.py\n"
            f"  → Update the frontend Zod schema to include these fields"
        )
        assert not removed, (
            f"\n{serializer_class.__name__}: fields removed from serializer.\n"
            f"  Removed: {sorted(removed)}\n"
            f"  → Re-run: poetry run python scripts/generate_contracts.py\n"
            f"  → Update the frontend Zod schema to remove these fields"
        )
