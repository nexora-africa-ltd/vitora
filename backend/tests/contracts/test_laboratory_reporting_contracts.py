"""
Serializer contract tests for laboratory_reporting app.

Auto-generated by scripts/generate_contracts.py — DO NOT edit field sets manually.
Re-run the script to update after serializer changes:
    poetry run python scripts/generate_contracts.py
"""

import pytest  # type: ignore

from hmis.apps.laboratory.reporting.serializers import (
    TATSLATargetCreateSerializer,
    TATSLATargetSerializer,
    TATSnapshotSerializer,
    WorkloadSnapshotSerializer,
)

CONTRACTS: list[tuple[type, frozenset[str]]] = [
    (
        TATSLATargetCreateSerializer,
        frozenset(
            {
                "breach_escalation_email",
                "is_active",
                "priority",
                "target_collect_to_receive_minutes",
                "target_order_to_collect_minutes",
                "target_receive_to_result_minutes",
                "target_result_to_verify_minutes",
                "target_total_minutes",
                "test",
            }
        ),
    ),
    (
        TATSLATargetSerializer,
        frozenset(
            {
                "breach_escalation_email",
                "created_at",
                "id",
                "is_active",
                "priority",
                "target_collect_to_receive_minutes",
                "target_order_to_collect_minutes",
                "target_receive_to_result_minutes",
                "target_result_to_verify_minutes",
                "target_total_minutes",
                "test",
                "test_code",
                "test_name",
                "updated_at",
            }
        ),
    ),
    (
        TATSnapshotSerializer,
        frozenset(
            {
                "breach_minutes",
                "collected_at",
                "id",
                "is_breach",
                "order_number",
                "ordered_at",
                "priority",
                "received_at",
                "released_at",
                "resulted_at",
                "resulted_by_name",
                "sla_target_minutes",
                "snapshot_created_at",
                "tat_collect_to_receive",
                "tat_order_to_collect",
                "tat_receive_to_result",
                "tat_result_to_verify",
                "tat_total",
                "test_code",
                "test_name",
                "verified_at",
                "verified_by_name",
            }
        ),
    ),
    (
        WorkloadSnapshotSerializer,
        frozenset(
            {
                "avg_entry_time_minutes",
                "avg_verify_time_minutes",
                "created_at",
                "critical_compliance_rate",
                "critical_notified_within_30min",
                "critical_results_count",
                "date",
                "id",
                "rejection_rate",
                "specimens_collected",
                "specimens_rejected",
                "technician",
                "technician_name",
                "tests_entered",
                "tests_verified",
            }
        ),
    ),
]


def _serializer_id(val):
    """Return readable test ID for parametrize."""
    return val.__name__ if isinstance(val, type) else None


@pytest.mark.django_db
class TestContractsLaboratoryReporting:
    """Contract tests for laboratory_reporting serializers."""

    @pytest.mark.parametrize(
        "serializer_class, expected_fields",
        CONTRACTS,
        ids=lambda val: _serializer_id(val),
    )
    def test_serializer_fields_match_contract(self, serializer_class, expected_fields):
        """Serializer field set must match the contract exactly."""
        actual_fields = frozenset(serializer_class().fields.keys())

        added = actual_fields - expected_fields
        removed = expected_fields - actual_fields

        assert not added, (
            f"\n{serializer_class.__name__}: new fields added to serializer.\n"
            f"  Added: {sorted(added)}\n"
            f"  → Re-run: poetry run python scripts/generate_contracts.py\n"
            f"  → Update the frontend Zod schema to include these fields"
        )
        assert not removed, (
            f"\n{serializer_class.__name__}: fields removed from serializer.\n"
            f"  Removed: {sorted(removed)}\n"
            f"  → Re-run: poetry run python scripts/generate_contracts.py\n"
            f"  → Update the frontend Zod schema to remove these fields"
        )
