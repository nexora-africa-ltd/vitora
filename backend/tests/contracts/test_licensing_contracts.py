"""
Serializer contract tests for licensing app.

Auto-generated by scripts/generate_contracts.py — DO NOT edit field sets manually.
Re-run the script to update after serializer changes:
    poetry run python scripts/generate_contracts.py
"""

import pytest  # type: ignore

from hmis.apps.licensing.serializers import (
    ActivationRequestSerializer,
    ActivationResponseSerializer,
    CheckInRequestSerializer,
    GenerateActivationCodeSerializer,
    InstallationDetailSerializer,
    InstallationListSerializer,
    LicenseStatusSerializer,
    RevokeSerializer,
)

CONTRACTS: list[tuple[type, frozenset[str]]] = [
    (
        ActivationRequestSerializer,
        frozenset(
            {
                "activation_code",
                "app_version",
                "eula_accepted",
                "eula_version",
                "installation_id",
                "name",
                "os_info",
            }
        ),
    ),
    (
        ActivationResponseSerializer,
        frozenset(
            {
                "check_in_by",
                "expires_at",
                "features",
                "installation_id",
                "license_token",
                "org_name",
                "tier",
            }
        ),
    ),
    (
        CheckInRequestSerializer,
        frozenset(
            {
                "app_version",
                "binary_hashes",
                "encounter_count_24h",
                "hardware_fingerprint",
                "hostname",
                "installation_id",
                "ip_address",
                "license_jti",
                "os_info",
                "uptime_seconds",
                "user_count_24h",
                "version",
            }
        ),
    ),
    (
        GenerateActivationCodeSerializer,
        frozenset(
            {
                "facility_id",
                "name",
                "organization_id",
            }
        ),
    ),
    (
        InstallationDetailSerializer,
        frozenset(
            {
                "activated_at",
                "activated_by",
                "activation_code",
                "app_version",
                "binary_manifest_id",
                "check_in_count",
                "check_in_ip",
                "created_at",
                "eula_accepted_at",
                "eula_version",
                "facility",
                "facility_name",
                "hardware_fingerprint",
                "hostname",
                "id",
                "installation_id",
                "is_tampered",
                "last_check_in",
                "last_reported_hashes",
                "name",
                "org_name",
                "organization",
                "os_info",
                "revocation_epoch",
                "revoked_at",
                "revoked_reason",
                "status",
                "tamper_flagged_at",
                "tamper_resolved_at",
                "updated_at",
            }
        ),
    ),
    (
        InstallationListSerializer,
        frozenset(
            {
                "activated_at",
                "app_version",
                "created_at",
                "id",
                "installation_id",
                "last_check_in",
                "name",
                "org_name",
                "os_info",
                "status",
            }
        ),
    ),
    (
        LicenseStatusSerializer,
        frozenset(
            {
                "check_in_by",
                "check_in_overdue",
                "error",
                "expires_at",
                "features",
                "org_name",
                "subscription_status",
                "tier",
                "valid",
            }
        ),
    ),
    (
        RevokeSerializer,
        frozenset(
            {
                "reason",
            }
        ),
    ),
]


def _serializer_id(val):
    """Return readable test ID for parametrize."""
    return val.__name__ if isinstance(val, type) else None


@pytest.mark.django_db
class TestContractsLicensing:
    """Contract tests for licensing serializers."""

    @pytest.mark.parametrize(
        "serializer_class, expected_fields",
        CONTRACTS,
        ids=lambda val: _serializer_id(val),
    )
    def test_serializer_fields_match_contract(self, serializer_class, expected_fields):
        """Serializer field set must match the contract exactly."""
        actual_fields = frozenset(serializer_class().fields.keys())

        added = actual_fields - expected_fields
        removed = expected_fields - actual_fields

        assert not added, (
            f"\n{serializer_class.__name__}: new fields added to serializer.\n"
            f"  Added: {sorted(added)}\n"
            f"  → Re-run: poetry run python scripts/generate_contracts.py\n"
            f"  → Update the frontend Zod schema to include these fields"
        )
        assert not removed, (
            f"\n{serializer_class.__name__}: fields removed from serializer.\n"
            f"  Removed: {sorted(removed)}\n"
            f"  → Re-run: poetry run python scripts/generate_contracts.py\n"
            f"  → Update the frontend Zod schema to remove these fields"
        )
