"""
Serializer contract tests for kenhdd app.

Auto-generated by scripts/generate_contracts.py — DO NOT edit field sets manually.
Re-run the script to update after serializer changes:
    poetry run python scripts/generate_contracts.py
"""
import pytest  # type: ignore

from hmis.apps.kenhdd.serializers import (
    KENHDDComplianceReportInputSerializer,
    KENHDDComplianceScoreSerializer,
    KENHDDDataElementDetailSerializer,
    KENHDDDataElementSerializer,
    KENHDDElementResultSerializer,
    KENHDDFailedRecordSerializer,
    KENHDDRecordResultSerializer,
    KENHDDRevalidateInputSerializer,
    KENHDDValidateRecordInputSerializer,
    KENHDDValidationRunDetailSerializer,
    KENHDDValidationRunSerializer,
)


CONTRACTS: list[tuple[type, frozenset[str]]] = [
    (
        KENHDDComplianceReportInputSerializer,
        frozenset({
            "resource_type",
            "sample_size",
        }),
    ),
    (
        KENHDDComplianceScoreSerializer,
        frozenset({
            "compliance_pct",
            "compliant_records",
            "mandatory_pass_rate",
            "resource_type",
            "total_elements",
            "total_records",
            "violations_by_element",
        }),
    ),
    (
        KENHDDDataElementDetailSerializer,
        frozenset({
            "coding_system",
            "condition_expression",
            "created_at",
            "data_type",
            "description",
            "element_id",
            "format_pattern",
            "id",
            "is_active",
            "max_length",
            "model_field",
            "name",
            "requirement_level",
            "resource_type",
            "updated_at",
        }),
    ),
    (
        KENHDDDataElementSerializer,
        frozenset({
            "coding_system",
            "data_type",
            "element_id",
            "id",
            "is_active",
            "model_field",
            "name",
            "requirement_level",
            "resource_type",
        }),
    ),
    (
        KENHDDElementResultSerializer,
        frozenset({
            "element_id",
            "element_name",
            "field_name",
            "message",
            "requirement_level",
            "status",
            "value",
        }),
    ),
    (
        KENHDDFailedRecordSerializer,
        frozenset({
            "fail_count",
            "id",
            "is_compliant",
            "pass_count",
            "record_exists",
            "record_id",
            "violation_details",
            "warning_count",
        }),
    ),
    (
        KENHDDRecordResultSerializer,
        frozenset({
            "elements",
            "fail_count",
            "is_compliant",
            "pass_count",
            "record_id",
            "resource_type",
            "warning_count",
        }),
    ),
    (
        KENHDDRevalidateInputSerializer,
        frozenset({
            "sample_size",
        }),
    ),
    (
        KENHDDValidateRecordInputSerializer,
        frozenset({
            "record_id",
            "resource_type",
        }),
    ),
    (
        KENHDDValidationRunDetailSerializer,
        frozenset({
            "compliance_score",
            "failed_records",
            "id",
            "mandatory_pass_rate",
            "records_checked",
            "records_compliant",
            "resource_type",
            "run_at",
            "run_by",
            "run_by_name",
            "total_failed",
            "violations",
        }),
    ),
    (
        KENHDDValidationRunSerializer,
        frozenset({
            "compliance_score",
            "id",
            "mandatory_pass_rate",
            "records_checked",
            "records_compliant",
            "resource_type",
            "run_at",
            "run_by",
            "run_by_name",
            "violations",
        }),
    ),
]


def _serializer_id(val):
    """Return readable test ID for parametrize."""
    return val.__name__ if isinstance(val, type) else None


@pytest.mark.django_db
class TestContractsKenhdd:
    """Contract tests for kenhdd serializers."""

    @pytest.mark.parametrize(
        "serializer_class, expected_fields",
        CONTRACTS,
        ids=lambda val: _serializer_id(val),
    )
    def test_serializer_fields_match_contract(self, serializer_class, expected_fields):
        """Serializer field set must match the contract exactly."""
        actual_fields = frozenset(serializer_class().fields.keys())

        added = actual_fields - expected_fields
        removed = expected_fields - actual_fields

        assert not added, (
            f"\n{serializer_class.__name__}: new fields added to serializer.\n"
            f"  Added: {sorted(added)}\n"
            f"  → Re-run: poetry run python scripts/generate_contracts.py\n"
            f"  → Update the frontend Zod schema to include these fields"
        )
        assert not removed, (
            f"\n{serializer_class.__name__}: fields removed from serializer.\n"
            f"  Removed: {sorted(removed)}\n"
            f"  → Re-run: poetry run python scripts/generate_contracts.py\n"
            f"  → Update the frontend Zod schema to remove these fields"
        )

