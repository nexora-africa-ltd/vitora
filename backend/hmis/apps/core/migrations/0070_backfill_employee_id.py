# Generated by Django 5.2.13 on 2026-06-15 23:00

import secrets
from datetime import date

from django.db import migrations


def backfill_employee_ids(apps, schema_editor):
    """Generate unique employee_id for staff profiles that have empty values."""
    StaffProfile = apps.get_model("core", "StaffProfile")
    empty_profiles = StaffProfile.objects.filter(employee_id="")
    year = date.today().year

    for profile in empty_profiles:
        for _ in range(10):
            candidate = f"VH-{year}-{secrets.token_hex(3).upper()}"
            if not StaffProfile.objects.filter(employee_id=candidate).exists():
                profile.employee_id = candidate
                profile.save(update_fields=["employee_id"])
                break
        else:
            # Fallback with longer hex
            profile.employee_id = f"VH-{year}-{secrets.token_hex(5).upper()}"
            profile.save(update_fields=["employee_id"])


class Migration(migrations.Migration):

    dependencies = [
        ("core", "0069_department_origin_hub_id_department_origin_local_id_and_more"),
    ]

    operations = [
        migrations.RunPython(backfill_employee_ids, migrations.RunPython.noop),
    ]
